# jsqrcode
Port jsqrcode(https://github.com/LazarSoft/jsqrcode) to module for webpack

# Documentation

setCallback(callback) - Callback function that will be called when decoded data
decode(url or data url) - Read the image and if data is found, callback is called